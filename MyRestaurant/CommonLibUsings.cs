// The shared contract (uCommonTypes) lives in CommonLib, which this module
// references per map-dependencies.md. Imported globally so no file needs to know.
global using CommonLib.Application.DTOs.Core;
